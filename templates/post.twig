{% extends "base.twig" %}

{% block body %}
    <div class="container">
        <img src="src/images/my-first-site-on-bs5.PNG" class="img-fluid mt-2" alt="...">
        <h1 class="mt-3 mb-3">{{ post.title|escape }}</h1>
        <div class="container mb-3">
            <div class="row">
                <span class="text-muted">Kirill Rubets | {{ post.published_date|date("F d, Y") }}</span>
            </div>
        </div>
        <div>
            {{ post.content|raw }}
        </div>
    </div>
{% endblock %}